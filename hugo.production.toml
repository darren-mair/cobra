# Legacy file kept for compatibility.
# Single-source config lives in config/_default/hugo.toml.
# Keep this value aligned with the live site if this file is ever used explicitly.
baseURL = "https://cobraboxingandfitness.com/"
