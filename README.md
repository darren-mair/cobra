# cobra
Cobra Site

## Stable Commands

Use the standard Hugo commands so local and live builds use the same config and path logic.

- Local dev server:
	- `hugo server`
- Production build:
	- `hugo`
